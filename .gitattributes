# Shell scripts and unit files are executed on Linux. Checked out with CRLF they
# fail with "bad interpreter: no such file or directory", which is a confusing
# way to learn about line endings while a deployment is on the clock.
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
