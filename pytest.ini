[pytest]
testpaths = tests
asyncio_mode = auto
# Live tests spend real quota, so they are opt-in: `pytest -m live`.
addopts = -q -m "not live"
markers =
    live: calls the real Gemini API and spends daily quota
filterwarnings =
    ignore::DeprecationWarning
